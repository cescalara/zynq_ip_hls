// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2016.2
// Copyright (C) 1986-2016 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xscurve_adder.h"

extern XScurve_adder_Config XScurve_adder_ConfigTable[];

XScurve_adder_Config *XScurve_adder_LookupConfig(u16 DeviceId) {
	XScurve_adder_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XSCURVE_ADDER_NUM_INSTANCES; Index++) {
		if (XScurve_adder_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XScurve_adder_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XScurve_adder_Initialize(XScurve_adder *InstancePtr, u16 DeviceId) {
	XScurve_adder_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XScurve_adder_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XScurve_adder_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

